/* AUTOGENERATED by scripts/gen-routes.mjs — do not edit by hand.
   Edit the page implementation in components/pages/, or the PAGES
   table in the generator, then re-run `npm run routes`. */
import type { Metadata } from 'next';
import { getTranslations, setRequestLocale } from 'next-intl/server';
import { SupportPage } from '@/components/pages';
import { buildMetadata, breadcrumbStructuredData, faqStructuredData } from '@/lib/seo';
import { defaultLocale } from '@/i18n/routing';
import { siteConfig } from '@/config/site.config';

export async function generateMetadata(): Promise<Metadata> {
  const typed = defaultLocale;
  const t = await getTranslations({ locale: typed, namespace: 'supportPage' });
  const tm = await getTranslations({ locale: typed, namespace: 'meta' });

  return buildMetadata({
    locale: typed,
    path: 'support',
    title: `${t('title')} — ${siteConfig.siteName}`,
    description: t('metaDescription'),
    ogImageAlt: tm('ogAlt'),
  });
}

export default async function Page() {
  const typed = defaultLocale;
  setRequestLocale(typed);

  const t = await getTranslations({ locale: typed, namespace: 'supportPage' });

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{
          __html: breadcrumbStructuredData(typed, siteConfig.siteName, {
            label: t('title'),
            path: 'support',
          }),
        }}
      />
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{
          __html: faqStructuredData(
            t.raw('faq') as { question: string; answer: string }[],
          ),
        }}
      />
      <SupportPage />
    </>
  );
}
